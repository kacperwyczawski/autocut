@using AutoCut.Core.Panels

@* padding on x axis have to be zero *@
<MudContainer Fixed="true" Class="px-0 my-4 gap-4 d-flex flex-row flex-wrap">
    @foreach (var panel in _panels)
    {
        <MudPaper>
            <MudStack Row="true" Class="pa-2" AlignItems="AlignItems.Center">
                <MudText>
                    <b>@panel.Length</b>
                </MudText>
                <MudText>
                    ✕
                </MudText>
                <MudText>
                    <b>@panel.Width</b>
                </MudText>
                <MudText>
                    ✕
                </MudText>
                <MudText>
                    <b>@panel.Quantity</b>
                </MudText>
                @* this wil be retrieved from panel *@
                <MudIcon Icon="@Icons.Material.Filled.BorderTop"/>
                <MudIcon Icon="@Icons.Material.Filled.BorderBottom"/>
                <MudIcon Icon="@Icons.Material.Filled.BorderLeft"/>
                <MudIcon Icon="@Icons.Material.Filled.BorderRight"/>
                <MudIconButton
                    Icon="@Icons.Material.Outlined.Delete"
                    Color="Color.Error"
                    Size="Size.Small"/>
            </MudStack>
        </MudPaper>
    }
</MudContainer>

@code
{
    // temporary data
    private readonly List<CompressedPanel> _panels = new()
    {
        new CompressedPanel(100, 100, 20),
        new CompressedPanel(60, 30, 2),
        new CompressedPanel(50, 50, 10),
        new CompressedPanel(50, 10, 20),
        new CompressedPanel(50, 300, 16),
        new CompressedPanel(100, 100, 20),
        new CompressedPanel(60, 30, 2),
        new CompressedPanel(50, 50, 10),
        new CompressedPanel(50, 10, 20),
        new CompressedPanel(50, 300, 16),
        new CompressedPanel(100, 100, 20),
        new CompressedPanel(60, 30, 2),
        new CompressedPanel(50, 50, 10),
        new CompressedPanel(50, 10, 20),
        new CompressedPanel(50, 300, 16)
    };
}