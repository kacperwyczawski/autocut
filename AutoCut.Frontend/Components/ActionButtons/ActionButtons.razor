@inject IDispatcher Dispatcher

<MudPaper Class="pa-4 mt-4">
    <MudButton
        Variant="Variant.Filled"
        Color="Color.Primary">
        Optimize
    </MudButton>
    <MudButton
        Variant="Variant.Filled"
        Color="Color.Error"
        OnClick="@(() => Dispatcher.Dispatch(new PanelResetAction()))">
        Reset all
    </MudButton>
</MudPaper>